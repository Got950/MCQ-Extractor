# Local dev: leave unset — Vite proxies /api to http://localhost:8000
# Netlify: set in Site settings → Environment variables (no trailing slash)
# VITE_API_URL=https://mcq-extractor-api.onrender.com/api
